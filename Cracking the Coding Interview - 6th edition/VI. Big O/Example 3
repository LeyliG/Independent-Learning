# print undordered pairs with the inner for loop starting at i+1
# O(N^2) runtime

def printUnorderedPairs(n=[]):
  for i in range(len(n)):
    for j in range(i+1, len(n)):
      print(n[i], ", ", n[j])
